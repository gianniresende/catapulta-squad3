import React from 'react';

import {Container, Title} from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <Title>Tema Configurado!</Title>
    </Container>
  );
};

export default Header;
